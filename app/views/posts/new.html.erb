<h1>New Post</h1>

<%= bootstrap_form_for @post do |f| %>
	<%= f.text_field :title %>
	<%= f.text_area :link %>

	<%= f.primary %>
<% end %>

<%= link_to "&larr Back", root_path, class: "btn btn-default" %>